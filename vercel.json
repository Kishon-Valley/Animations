{
  "framework": "nextjs",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "buildCommand": "pnpm run build",
  "outputDirectory": ".next",
  "env": {
    "NODE_VERSION": "18.x",
    "PNPM_FLAGS": "--shamefully-hoist --strict-peer-dependencies=false"
  }
}
